{
  "name": "running-dart-firestore-rules",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --config firebase.rules-test.json --project running-dart-test --only firestore \"node tests/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0"
  }
}
